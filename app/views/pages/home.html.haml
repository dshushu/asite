%h1 Home

%p
  = link_to :Recipes, recipes_path, class:"btn btn-info"
  = link_to :Chefs, chefs_path, class: "btn btn-info"
  - if logged_in?
    = link_to :Logout, logout_path, class: 'btn btn-warning'
  - else
    = link_to :Login, login_path, class: 'btn btn-success'
    = link_to :Register, register_path, class: 'btn btn-info'
